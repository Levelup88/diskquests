{
  "name": "diskquest-store",
  "private": true,
  "version": "1.0.1",
  "description": "DiskQuest Store static web app",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site: no build step required'",
    "preview": "npx serve . -l 5500"
  }
}
